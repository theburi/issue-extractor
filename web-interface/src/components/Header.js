import React from 'react';
import './Header.css'; // Ensure this file exists in the same directory

const Header = () => {
    return (
        <header className="header">
            <h1>Issue Extractor</h1>
        </header>
    );
};

export default Header;